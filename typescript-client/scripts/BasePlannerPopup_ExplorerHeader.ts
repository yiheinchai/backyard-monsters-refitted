import MovieClip from 'openfl/display/MovieClip';
import TextField from 'openfl/text/TextField';

/**
 * BasePlannerPopup_ExplorerHeader - Base UI clip class for Base Planner Explorer Header
 * Contains all UI element declarations for the explorer header
 * Converted from ActionScript to TypeScript
 */
export class BasePlannerPopup_ExplorerHeader extends MovieClip {
    public tLabel!: TextField;

    constructor() {
        super();
    }
}
