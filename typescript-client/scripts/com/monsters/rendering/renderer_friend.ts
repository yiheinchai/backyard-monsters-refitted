/**
 * The renderer_friend namespace in ActionScript provided package-internal visibility.
 * In TypeScript, we use standard public/protected/private modifiers instead.
 * This file exists for reference but the namespace concept is replaced by
 * direct property access with appropriate visibility modifiers.
 */
export const renderer_friend = Symbol("renderer_friend");
