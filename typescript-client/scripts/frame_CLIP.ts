import { frame } from './frame';

/**
 * frame_CLIP - Embedded frame clip
 * Extends frame for asset embedding
 * Converted from ActionScript to TypeScript
 */
export class frame_CLIP extends frame {
    constructor() {
        super();
    }
}
